#include <bits/stdc++.h>
using namespace std;

void solve()
{
    long long int a, b;
    cin >> a >> b;

    cout << a + b << '\n';
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    long long int T = 1;
    cin >> T;
    while (T--) 
    {
        solve();
    }

}